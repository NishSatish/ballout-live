import { StyleSheet } from 'react-native';

// GLOBAL STYLE ASSETS
const appBG = '#2c2c2c';

export const GlobalStyles = StyleSheet.create({
  screenBG: {
    backgroundColor: appBG,
    height: '100%',
  },
});
